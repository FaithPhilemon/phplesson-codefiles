<?php 
    
    /*

    1. strlen()	Returns the length of a string
    
    2. str_word_count()	Count the number of words in a string
    
    3. trim()	Removes whitespace or other characters from both sides of a string
    
    4. ucwords()	Converts the first character of each word in a string to uppercase
    
    5. strtoupper()	Converts a string to uppercase letters


    “Those who educate children well are more to be honored than they who produce them; 
     for these only gave them life, those the art of living well.”― Aristotle

    */

    $quote = "Those who educate children well are more to be honored than they who produce them; 
    for these only gave them life, those the art of living well.― Aristotle";

    // echo strlen($quote);

    // echo str_word_count($quote);

    // echo ucwords($quote);

    // echo strtoupper($quote);
?>